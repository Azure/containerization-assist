# NOTE: legacy practice — credentials baked into the WAR.
# Containerization should externalize these via env vars / k8s Secrets.
db.driver=com.mysql.jdbc.Driver
db.url=jdbc:mysql://db.internal.corp:3306/legacy_app?useSSL=false
db.username=legacy_app
db.password=changeme

app.name=Legacy Internal App
app.environment=production
