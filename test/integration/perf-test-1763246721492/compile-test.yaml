apiVersion: policy.containerization-assist.dev/v1
kind: PolicySet
metadata:
  name: compile-test
spec:
  rules:
    - name: test-rule
      category: test
      severity: block
      condition: 'input.content.contains("FROM")'
      message: "Test"
